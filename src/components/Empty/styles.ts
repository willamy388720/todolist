import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyComponent: {
    borderTopWidth: 1,
    borderTopColor: "#333333",
    paddingTop: 48,
    alignItems: "center",
  },
  emptyText: {
    fontSize: 16,
    color: "#808080",
  },
});
